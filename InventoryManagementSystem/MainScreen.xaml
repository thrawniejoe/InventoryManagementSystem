<Window x:Class="InventoryManagementSystem.MainScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventoryManagementSystem"
        mc:Ignorable="d"
        Title="MainScreen" Height="768" Width="1024">

    <Window.Resources>
    </Window.Resources>
    <Grid>

        <TabControl TabStripPlacement="Left" >
            <TabControl.Resources>
                <Style TargetType="{x:Type TabItem}">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <ContentPresenter Content="{TemplateBinding Content}">
                                    <ContentPresenter.LayoutTransform>
                                        <RotateTransform Angle="270" />
                                    </ContentPresenter.LayoutTransform>
                                </ContentPresenter>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Padding" Value="3" />
                </Style>
            </TabControl.Resources>
            <TabItem Header="Inventory" Margin="0,60,0,-60">
                <TabControl Width="976" Margin="0,39,0,7">
                    <TabControl.Resources>
                        <Style TargetType="{x:Type TabItem}">
                            <Setter Property="HeaderTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <ContentPresenter Content="{TemplateBinding Content}">
                                            <ContentPresenter.LayoutTransform>
                                                <RotateTransform Angle="0" />
                                            </ContentPresenter.LayoutTransform>
                                        </ContentPresenter>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Padding" Value="3" />
                        </Style>
                    </TabControl.Resources>
                    <TabItem Header="Inventory List">
                        <Grid Background="#FFE5E5E5">
                            <WrapPanel HorizontalAlignment="Left" Height="35" Margin="10,10,0,0" VerticalAlignment="Top" Width="950">
                                <Button x:Name="btnAddItem" Content="Add Item" Width="75"/>
                                <Button x:Name="btnDeleteItem" Content="Delete" Width="75"/>
                                <Button x:Name="btnClearFilter" Content="Clear Filter" Width="75"/>
                                <ComboBox Width="120"/>
                            </WrapPanel>
                            <DataGrid x:Name="gvInventoryList" HorizontalAlignment="Left" Height="595" Margin="10,50,0,0" VerticalAlignment="Top" Width="950"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Item Details">
                        <Grid Background="#FFE5E5E5"/>
                    </TabItem>
                </TabControl>
            </TabItem>
            <TabItem Header="Personnel" Margin="0,60,0,-60" />
            <TabItem Header="Building Map" Margin="0,60,0,-60" />
            <TabItem Header="Reports" Margin="0,60,0,-60" />
            <TabItem Header="Settings" Margin="-2,60,0,-60" />
        </TabControl>
    </Grid>
</Window>
